// Add verified client testimonials here once available.
// The Testimonials component automatically hides itself when this array is empty —
// do not add placeholder or invented reviews.
export type Testimonial = {
  quote: string;
  attribution: string;
};

export const testimonials: Testimonial[] = [];
